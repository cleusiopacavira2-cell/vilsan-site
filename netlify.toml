[build]
  command = "npm run build"
  functions = ".netlify/functions"
  publish = ".next"

[functions]
  node_bundler = "esbuild"

# Desabilitar secrets scanning para evitar falsos positivos
[build.environment]
  SECRETS_SCAN_ENABLED = "false"
